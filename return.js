function tenTimes(number) {
  const result = number * 10;
  return result;
}

function cutHalf(number) {
  const result = number / 2;
  return result;
}

let output = tenTimes(20);
console.log(output);

let halfValue = cutHalf(20);
console.log(halfValue);
